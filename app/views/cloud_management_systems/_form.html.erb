<%= form_with(model: cloud_management_system) do |form| %>
  <% if cloud_management_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cloud_management_system.errors.count, "error") %> prohibited this cloud_management_system from being saved:</h2>

      <ul>
        <% cloud_management_system.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
