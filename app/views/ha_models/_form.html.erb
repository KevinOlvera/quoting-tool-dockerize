<%= form_with(model: ha_model) do |form| %>
  <% if ha_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ha_model.errors.count, "error") %> prohibited this ha_model from being saved:</h2>

      <ul>
        <% ha_model.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
